

/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build init') {
            steps {
                sh 'cd demo/django/init && docker build --tag Django_db_init:latest . '
            }
        }
        stage('build app') {
            steps {
                sh 'cd demo/django/app && docker build --tag Django_app:latest . '

            }
        }
    }
}
